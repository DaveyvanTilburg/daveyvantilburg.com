@using System.Linq;
@model DaveyVanTilburgWebsite.Models.Scribblings

@{
    ViewBag.Title = "Posts";
}

<div>
    <ul class="colorPalletedList">
        @foreach (Scribbling item in Model.Items().OrderByDescending(i => i.Name()))
        {
            <li>
                @Html.ActionLink(item.Name(), "ViewItem", "Scribblings", new { name = item.Name() }, new { @class = "text-dark" })
            </li>
        }
    </ul>
</div>

@section css {
    <link rel="stylesheet" href="~/css/UnorderedList.css" />
}